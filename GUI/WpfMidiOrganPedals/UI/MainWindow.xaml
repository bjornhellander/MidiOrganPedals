<Window x:Class="WpfMidiOrganPedals.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMidiOrganPedals.UI"
        mc:Ignorable="d"
        Title="MIDI Organ Pedals"
        Height="350"
        Width="1300"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Border BorderBrush="Black" BorderThickness="2" Margin="0,0,0,5" Grid.Row="0">
            <local:GeneralStatusView/>
        </Border>
        
        <Border BorderBrush="Black" BorderThickness="2" Margin="0,0,0,5" Grid.Row="1">
            <local:MessagesView/>
        </Border>
    </Grid>
</Window>
